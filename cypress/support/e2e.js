// Support file for Cypress tests
// You can add custom commands here if needed in the future

// Example custom command (uncomment to use):
// Cypress.Commands.add('selectSubnet', (cidr) => {
//   cy.get(`[data-cidr="${cidr}"]`).click();
// });
